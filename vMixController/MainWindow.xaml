<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:cls="clr-namespace:vMixController.Classes"
        xmlns:c="clr-namespace:vMixController.Converters"
        xmlns:ctrls="clr-namespace:vMixController.Controls"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:props="clr-namespace:vMixController.Properties"
        xmlns:loc="clr-namespace:vMixControllerSkin.Localization;assembly=vMixControllerSkin"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        
        xmlns:e="clr-namespace:vMixController.Extensions"
        xmlns:toolkit="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:this="clr-namespace:vMixController"
        xmlns:vm="clr-namespace:vMixController.ViewModel"
        xmlns:skin="clr-namespace:vMixControllerSkin;assembly=vMixControllerSkin"
        x:Class="vMixController.MainWindow"
        xmlns:wpfc="clr-namespace:NKristek.Wpf.Converters;assembly=NKristek.Wpf.Converters"
        mc:Ignorable="d"
        x:Name="Window"
        
        Left="{Binding WindowSettings.Left, Mode=TwoWay}"
        Top="{Binding WindowSettings.Top, Mode=TwoWay}"
        Width="{Binding WindowSettings.Width, Mode=TwoWay}"
        Height="{Binding WindowSettings.Height, Mode=TwoWay}"
        WindowState="{Binding WindowSettings.State, Mode=TwoWay}"
        MinWidth="400"
        MinHeight="400"
        Title="{Binding Title}"
        ResizeMode="CanResizeWithGrip" d:DesignWidth="1016.552" d:DesignHeight="500"  Loaded="Window_Loaded"
        >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/vMixControllerSkin;component/MainSkin.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Rect x:Key="IconViewbox">0.18,0.18,0.64,0.64</Rect>
            <c:Int32ToVisibilityConverter x:Key="Int32ToVisibilityConverter"/>
            <Storyboard x:Key="StoryboardFadeIn">
                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                    <DiscreteObjectKeyFrame KeyTime="0">
                        <DiscreteObjectKeyFrame.Value>
                            <Visibility>Visible</Visibility>
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>
                </ObjectAnimationUsingKeyFrames>
                <DoubleAnimation Duration="0:0:0.5" To="1" Storyboard.TargetProperty="Opacity"/>
            </Storyboard>

            <Storyboard x:Key="StoryboardFadeOut">
                <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="Visibility">
                    <DiscreteObjectKeyFrame KeyTime="0:0:0.5">
                        <DiscreteObjectKeyFrame.Value>
                            <Visibility>Collapsed</Visibility>
                        </DiscreteObjectKeyFrame.Value>
                    </DiscreteObjectKeyFrame>
                </ObjectAnimationUsingKeyFrames>
                <DoubleAnimation Duration="0:0:0.5" To="0" Storyboard.TargetProperty="Opacity"/>
            </Storyboard>

            <CompositeCollection x:Key="SubMenu" x:Shared="False">
                <MenuItem Icon="{x:Null}" ItemsSource="{x:Null}" Header="{loc:Loc Key=MainWindow.SubMenu.AddWidget}" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.CreateWidgetFromTemplateCommand}" CommandParameter="{Binding}"/>
                <Separator/>
                <MenuItem Icon="{x:Null}" ItemsSource="{x:Null}" Header="{loc:Loc Key=MainWindow.SubMenu.EditTemplate}" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.EditWidgetTemplateCommand}" CommandParameter="{Binding}"/>
                <MenuItem Icon="{x:Null}" ItemsSource="{x:Null}" Header="{loc:Loc Key=MainWindow.SubMenu.RemoveTemplate}" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.RemoveWidgetTemplateCommand}" CommandParameter="{Binding}"/>
            </CompositeCollection>
            <Rectangle x:Key="DynamicIcon" x:Shared="False" Width="16" Height="16" StrokeThickness="1"
					   Fill="{Binding B.Color, FallbackValue=Black, Converter={c:ColorToSolidBrushConverter}}"
					   Stroke="{Binding B.BorderColor, FallbackValue=Black, Converter={c:ColorToSolidBrushConverter}}"/>
            <Style TargetType="Border" x:Key="IconBorder">
                <Setter Property="BorderBrush" Value="Black"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
            <CompositeCollection x:Key="CreateItems" x:Shared="False">
                <!--DEPRECATED-->
                <!--<MenuItem x:Name="Button" Command="{Binding CreateWidgetCommand}" CommandParameter="Button" Header="{loc:Loc Key=MainWindow.CreateItems.Button}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF205;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>-->
                <MenuItem x:Name="NewButton" Command="{Binding CreateWidgetCommand}" CommandParameter="NewButton" Header="{loc:Loc Key=MainWindow.CreateItems.NewButton}">
                    <MenuItem.Icon>
                        <Border Style="{StaticResource IconBorder}">
                            <Grid Width="16" Height="16">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF205;</TextBlock>
                                <Path Grid.Column="1" x:Name="triangle" 
                      Data="M8,0 L8,8 L0,8 Z" 
                      Fill="#d77d31" 
                      HorizontalAlignment="Right"
                      VerticalAlignment="Bottom"
                      Opacity="1"/>
                            </Grid>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Label" Command="{Binding CreateWidgetCommand}" CommandParameter="Label" Header="{loc:Loc Key=MainWindow.CreateItems.Label}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF02B;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="VariableViewer" Command="{Binding CreateWidgetCommand}" CommandParameter="VariableViewer" Header="{loc:Loc Key=MainWindow.CreateItems.VariableViewer}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF02B;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Region" Command="{Binding CreateWidgetCommand}" CommandParameter="Region" Header="{loc:Loc Key=MainWindow.CreateItems.Region}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF2D0;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <!--<MenuItem Command="{Binding CreateWidgetCommand}" CommandParameter="Image" Header="Image"/>-->
                <MenuItem x:Name="List" Command="{Binding CreateWidgetCommand}" CommandParameter="List" Header="{loc:Loc Key=MainWindow.CreateItems.List}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF03A;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Score" Command="{Binding CreateWidgetCommand}" CommandParameter="Score" Header="{loc:Loc Key=MainWindow.CreateItems.Score}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF005;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="TextField" Command="{Binding CreateWidgetCommand}" CommandParameter="TextField" Header="{loc:Loc Key=MainWindow.CreateItems.TextField}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF031;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Timer" Command="{Binding CreateWidgetCommand}" CommandParameter="Timer" Header="{loc:Loc Key=MainWindow.CreateItems.Timer}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xE06F;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Clock" Command="{Binding CreateWidgetCommand}" CommandParameter="Clock" Header="{loc:Loc Key=MainWindow.CreateItems.Clock}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF34E;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Volume" Command="{Binding CreateWidgetCommand}" CommandParameter="Volume" Header="{loc:Loc Key=MainWindow.CreateItems.Volume}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF1DE;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="TBar" Command="{Binding CreateWidgetCommand}" CommandParameter="TBar" Header="{loc:Loc Key=MainWindow.CreateItems.TBar}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">T</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="Playlist" Command="{Binding CreateWidgetCommand}" CommandParameter="Playlist" Header="{loc:Loc Key=MainWindow.CreateItems.Playlist}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xf00b;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="Container" Command="{Binding CreateWidgetCommand}" CommandParameter="Container" Header="{loc:Loc Key=MainWindow.CreateItems.Container}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF1B2;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="ExternalData" Command="{Binding CreateWidgetCommand}" ItemsSource="{Binding ExternalDataProviders}" CommandParameter="ExternalData" Header="{loc:Loc Key=MainWindow.CreateItems.ExternalData}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF1C0;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                    <MenuItem.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding A}"/>
                        </DataTemplate>
                    </MenuItem.ItemTemplate>
                    <MenuItem.Resources>
                        <Style BasedOn="{StaticResource {x:Type MenuItem}}" TargetType="{x:Type MenuItem}">
                            <Setter Property="Command" Value="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.CreateWidgetFromTemplateCommand}"/>
                            <Setter Property="CommandParameter" Value="{Binding}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Icon" Value="{StaticResource DynamicIcon}"/>
                        </Style>
                    </MenuItem.Resources>
                </MenuItem>
                <MenuItem x:Name="MidiInterface" Command="{Binding CreateWidgetCommand}" CommandParameter="MidiInterface" Header="{loc:Loc Key=MainWindow.CreateItems.MidiDevice}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF0E7;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="StreamDeck" Command="{Binding CreateWidgetCommand}" CommandParameter="StreamDeck" Header="{loc:Loc Key=MainWindow.CreateItems.StreamDeckDevice}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF11C;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <MenuItem x:Name="MultiState" Command="{Binding CreateWidgetCommand}" CommandParameter="MultiState" Header="{loc:Loc Key=MainWindow.CreateItems.MultiState}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16" Style="{StaticResource IconBorder}">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF233;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem x:Name="Templates" Header="{loc:Loc Key=MainWindow.CreateItems.Templates}" ItemsSource="{Binding WidgetTemplates}" IsEnabled="{Binding Items.Count, Converter={wpfc:ObjectToStringEqualsParameterToInverseBoolConverter}, ConverterParameter=0, Mode=OneWay, RelativeSource={RelativeSource Self}}">

                    <MenuItem.ItemTemplate>
                        <DataTemplate>
                            <TextBlock><Run Text="["/><InlineUIContainer>
									<TextBlock FontStyle="Italic" Text="{Binding B.Type, Mode=OneWay}"/>
								</InlineUIContainer><Run Text="] "/><InlineUIContainer>
									<TextBlock Text="{Binding A}"/>
								</InlineUIContainer></TextBlock>
                        </DataTemplate>
                    </MenuItem.ItemTemplate>
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="{x:Type MenuItem}">
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="ItemsSource" Value="{StaticResource SubMenu}"/>
                            <Setter Property="Icon" Value="{StaticResource DynamicIcon}"/>
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>
            </CompositeCollection>
        </ResourceDictionary>
    </Window.Resources>

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="PreviewKeyUp">
            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding PreviewKeyUpCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="PreviewKeyDown">
            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding PreviewKeyDownCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="PreviewMouseDown">
                    <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding PreviewMouseDownCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="PreviewMouseUp">
                    <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding PreviewMouseUpCommand}"/>
        </i:EventTrigger>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding ClosingCommand}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <i:Interaction.Behaviors>
        <e:HideCloseButtonOnWindow IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}"/>
    </i:Interaction.Behaviors>
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Menu KeyboardNavigation.TabNavigation="None" IsTabStop="False" VerticalContentAlignment="Center" HorizontalContentAlignment="Left">
            <MenuItem Header="{loc:Loc Key=MainWindow.Menu.File}">
                <MenuItem Command="{Binding NewControllerCommand}" Header="{loc:Loc Key=MainWindow.Menu.File.New}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF016;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Command="{Binding LoadControllerCommand}" Header="{loc:Loc Key=MainWindow.Menu.File.Open}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF07C;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="{loc:Loc Key=MainWindow.Menu.File.RecentFiles}" ItemsSource="{Binding WindowSettings.RecentFiles}" Visibility="{Binding WindowSettings.RecentFiles, Converter={wpfc:ICollectionNullOrEmptyToInverseVisibilityConverter}}">
                    <MenuItem.Resources>
                        <ResourceDictionary>
                            <Border x:Key="Icon" x:Shared="false"  Width="16" Height="16">
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF15B;</TextBlock>
                            </Border>
                        </ResourceDictionary>
                    </MenuItem.Resources>
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF07C;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="{x:Type MenuItem}">
                            <Setter Property="Command" Value="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.LoadRecentControllerCommand}"/>
                            <Setter Property="CommandParameter" Value="{Binding}"/>
                            <Setter Property="Icon" Value="{StaticResource Icon}">
                            </Setter>
                        </Style>
                    </MenuItem.ItemContainerStyle>
                </MenuItem>

                <MenuItem Command="{Binding AppendControllerCommand}" Header="{loc:Loc Key=MainWindow.Menu.File.Append}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <Grid>
                                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF07C;</TextBlock>
                                <TextBlock Foreground="White" FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontFamily="{StaticResource FontAwesome}">&#xF111;</TextBlock>
                                <TextBlock FontSize="10" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontFamily="{StaticResource FontAwesome}">&#xF055;</TextBlock>
                            </Grid>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Command="{Binding SaveControllerCommand}"  Header="{loc:Loc Key=MainWindow.Menu.File.Save}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF0C7;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <!--<MenuItem Command="{Binding GlobalVariablesCommand}" Header="Global Variables">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF02D;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>-->
                <MenuItem Command="{Binding DownloadUpdateCommand}">
                    <MenuItem.Header>
                        <TextBlock FontWeight="Bold"><Run Text="{loc:Loc Key=MainWindow.Menu.File.DownloadUpdate.Prefix}" /><Run Text=" [" /><Run Text="{Binding AvailableVersion, StringFormat=MM/dd/yyy}" /><Run Text="] " /><Run Text="{loc:Loc Key=MainWindow.Menu.File.DownloadUpdate.Suffix}" /></TextBlock>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF019;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UpdateLink}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>
                <!--<MenuItem Command="{Binding ViewChangelogCommand}">
                    <MenuItem.Header>
                        <TextBlock FontWeight="Bold">View Changelog</TextBlock>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF15C;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                    <MenuItem.Style>
                        <Style TargetType="MenuItem">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding UpdateLink}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                </MenuItem>-->
                <MenuItem Command="{Binding AboutCommand}" Header="{loc:Loc Key=MainWindow.Menu.File.About}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF05A;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator/>
                <MenuItem Command="{Binding ExitCommand}"  Header="{loc:Loc Key=MainWindow.Menu.File.Exit}">
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF08B;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="{loc:Loc Key=MainWindow.Menu.Language}" ItemsSource="{x:Static loc:LocalizationManager.Locales}" Click="LanguageMenuItem_Click">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Style.Setters>
                            <Setter Property="Header" Value="{Binding NativeName}"/>
                            <Setter Property="Tag" Value="{Binding}"/>
                            <Setter Property="IsChecked">
                                <Setter.Value>
                                    <MultiBinding Converter="{c:MultipleValueEqualsConverter}">
                                        <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Culture"/>
                                        <Binding RelativeSource="{RelativeSource Self}" Path="Tag"/>
                                    </MultiBinding>
                                </Setter.Value>
                            </Setter>
                        </Style.Setters>
                    </Style>
                </MenuItem.ItemContainerStyle>
                <!--<MenuItem IsChecked="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Culture, Converter={wpfc:ObjectToStringEqualsParameterToBoolConverter}, ConverterParameter=en-US}" Header="{loc:Loc Key=MainWindow.Menu.Language.English}" Tag="en-US" Click="LanguageMenuItem_Click" />
                <MenuItem IsChecked="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Culture, Converter={wpfc:ObjectToStringEqualsParameterToBoolConverter}, ConverterParameter=zh-CN}" Header="{loc:Loc Key=MainWindow.Menu.Language.Chinese}" Tag="zh-CN" Click="LanguageMenuItem_Click" />
                <MenuItem IsChecked="{Binding Source={x:Static loc:LocalizationManager.Instance}, Path=Culture, Converter={wpfc:ObjectToStringEqualsParameterToBoolConverter}, ConverterParameter=ru-RU}" Header="{loc:Loc Key=MainWindow.Menu.Language.Russian}" Tag="ru-RU" Click="LanguageMenuItem_Click" />-->
            </MenuItem>
            <MenuItem IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Header="{loc:Loc Key=MainWindow.Menu.Edit}">
                <MenuItem IsEnabled="{Binding UndoState, Converter={wpfc:ValueNullToInverseBoolConverter}}" Command="{Binding UndoCommand}">
                    <MenuItem.Header>
                        <StackPanel>
                            <TextBlock Text="{loc:Loc Key=MainWindow.Menu.Edit.Undo}"/>
                            <TextBlock Foreground="Gray" FontSize="10" Text="{Binding UndoReason}" Visibility="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=MenuItem, Mode=FindAncestor}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF0E2;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Command="{Binding DuplicateSelectedCommand}">
                    <MenuItem.Header>
                        <TextBlock Text="{loc:Loc Key=MainWindow.Menu.Edit.DuplicateSelected}"/>
                    </MenuItem.Header>
                    <MenuItem.Icon>
                        <Border Width="16" Height="16">
                            <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}">&#xF0c5;</TextBlock>
                        </Border>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Header="{loc:Loc Key=MainWindow.Menu.Widget}" ItemsSource="{DynamicResource CreateItems}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            <MenuItem ToolTip="{loc:Loc Key=MainWindow.Menu.Lock.ToolTip}" Header="{Binding WindowSettings.Locked}" Command="{Binding ToggleLockCommand}">
                <MenuItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Width="16px" VerticalAlignment="Center" HorizontalAlignment="Center" FontFamily="{StaticResource FontAwesome}" x:Name="LockedIcon"></TextBlock>
                            <TextBlock Text="{loc:Loc Key=MainWindow.Menu.Lock}"/>
                        </StackPanel>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding}" Value="True">
                                <Setter Property="Text" Value="&#xF023;" TargetName="LockedIcon"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding}" Value="False">
                                <Setter Property="Text" Value="&#xF3C1;" TargetName="LockedIcon"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </MenuItem.HeaderTemplate>
            </MenuItem>
            <MenuItem ToolTip="{loc:Loc Key=MainWindow.Menu.Sync.ToolTip}" Header="{Binding Status}" Command="{Binding SyncStateCommand}">
                <MenuItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Margin="0,0,8,0" Stroke="Black">
                                <Ellipse.Fill>
                                    <SolidColorBrush Color="{Binding Path=., Converter={c:StatusToColorConverter}}"/>
                                </Ellipse.Fill>
                            </Ellipse>
                            <TextBlock Text="{loc:Loc Key=MainWindow.Menu.Sync}"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </MenuItem.HeaderTemplate>
                <MenuItem.Style>
                    <Style TargetType="MenuItem">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="Sync">
                                <Setter Property="FontWeight" Value="Bold"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </MenuItem.Style>
            </MenuItem>
        </Menu>
        <Grid Grid.Row="1" Background="{StaticResource ListBoxBackgroundBrush}">

            <TabControl SelectedIndex="{Binding SelectedTab, Mode=TwoWay}" BorderThickness="0" Background="#1e2328">
                <TabItem IsTabStop="False" Style="{StaticResource GrayItemStyle}" Header="{loc:Loc Key=MainWindow.Tab.Settings}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}">
                    <StackPanel Margin="{StaticResource StdMargin}">
                        <Grid Margin="{StaticResource TopBottomStdMargin}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource PropertyName}" Margin="{StaticResource RightStdMargin}" FontSize="14"><Run Text="{loc:Loc Key=MainWindow.Settings.WindowProperties}"/></TextBlock>
                            <Border VerticalAlignment="Center" Grid.Column="1" BorderThickness="0,2,0,0" BorderBrush="White"/>
                        </Grid>

                        <skin:PropertiesListView KeyboardNavigation.TabNavigation="Local" KeyboardNavigation.DirectionalNavigation="Contained" HorizontalContentAlignment="Stretch" Background="Transparent" BorderThickness="0">
                            <!--<ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel HorizontalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <ContentPresenter/>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="{StaticResource StdMargin}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="2*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid Background="{StaticResource Main.Gradient}">
                                            <TextBlock Margin="8,0,0,0" Padding="{StaticResource StdMargin}" Style="{StaticResource PropertyName}"><Run Text="{Binding Tag}"/></TextBlock>
                                        </Grid>
                                        <ContentPresenter Grid.Column="1" Content="{Binding}"/>
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>-->
                            <CompositeCollection>
                                <TextBox TabIndex="1" Style="{StaticResource WidgetTextBox}" Tag="{loc:Loc Key=MainWindow.Settings.Field.IP}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Text="{Binding WindowSettings.IP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" BorderBrush="{Binding IsUrlValid, Converter={c:BoolToSolidColorBrushConverter}, ConverterParameter=1}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewKeyUp">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="GotFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="LostFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <TextBox TabIndex="2" Style="{StaticResource WidgetTextBox}" Tag="{loc:Loc Key=MainWindow.Settings.Field.Port}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Text="{Binding WindowSettings.Port, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" BorderBrush="{Binding IsUrlValid, Converter={c:BoolToSolidColorBrushConverter}, ConverterParameter=1}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewKeyUp">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="GotFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="LostFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>

                                <TextBox TabIndex="3" Style="{StaticResource WidgetTextBox}" Tag="{loc:Loc Key=MainWindow.Settings.Field.ApiLogin}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Text="{Binding WindowSettings.HttpLogin, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewKeyUp">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="GotFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="LostFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>
                                <TextBox TabIndex="4" Style="{StaticResource WidgetTextBox}" Tag="{loc:Loc Key=MainWindow.Settings.Field.ApiPassword}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Text="{Binding WindowSettings.HttpPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewKeyUp">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="GotFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="LostFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>


                                <TextBox TabIndex="5" Style="{StaticResource WidgetTextBox}" Tag="{loc:Loc Key=MainWindow.Settings.Field.PollTime}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}" Text="{Binding WindowSettings.PollTime, Mode=TwoWay}">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewKeyUp">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="GotFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                        </i:EventTrigger>
                                        <i:EventTrigger EventName="LostFocus">
                                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </TextBox>

                                <Grid KeyboardNavigation.TabNavigation="Local" KeyboardNavigation.DirectionalNavigation="Contained" Tag="{loc:Loc Key=MainWindow.Settings.Field.Scaling}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="Auto" MinWidth="64"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Slider TabIndex="6" Value="{Binding WindowSettings.UIScale, Mode=TwoWay}" Minimum="0.5" Maximum="5"/>
                                    <TextBlock Grid.Column="1" Style="{StaticResource PropertyName}" Margin="{StaticResource LeftStdMargin}" Text="{Binding WindowSettings.UIScale, StringFormat=0.00}"/>
                                    <Button TabIndex="7" Grid.Column="2" Padding="8,0" Command="{Binding ResetScalingCommand}" Content="{loc:Loc Key=MainWindow.Settings.Button.Reset}" />
                                </Grid>
                                <CheckBox TabIndex="8" Tag="{loc:Loc Key=MainWindow.Settings.Field.OpenLastAtStart}" IsChecked="{Binding WindowSettings.OpenLastAtStart, Mode=TwoWay}"/>
                                <CheckBox TabIndex="9" Tag="{loc:Loc Key=MainWindow.Settings.Field.IsTopmost}" IsChecked="{Binding WindowSettings.IsTopmost, Mode=TwoWay}"/>
                                <Border Tag="{loc:Loc Key=MainWindow.Settings.Field.EnableLog}">
                                    <StackPanel Orientation="Horizontal">
                                        <CheckBox TabIndex="10" IsChecked="{Binding WindowSettings.EnableLog, Mode=TwoWay}"/>
                <Button TabIndex="11" Margin="4,0,0,0" VerticalAlignment="Center" FontSize="10" Height="16" Command="{Binding OpenLogFolderCommand}" Content="{loc:Loc Key=MainWindow.Settings.Button.OpenLogFolder}" />
                                    </StackPanel>
                                </Border>

                                <CheckBox TabIndex="12" Tag="{loc:Loc Key=MainWindow.Settings.Field.ShowWidgetLock}" IsChecked="{Binding WindowSettings.ShowIndividualLock, Mode=TwoWay}"/>
                                <CheckBox TabIndex="13" Tag="{loc:Loc Key=MainWindow.Settings.Field.BlinkLightsNeedRestart}" IsChecked="{Binding WindowSettings.BlinkLights, Mode=TwoWay}" Cursor=""/>
                                <CheckBox TabIndex="14" Tag="{loc:Loc Key=MainWindow.Settings.Field.UseInfiniteCanvas}" IsChecked="{Binding WindowSettings.UseInfiniteCanvas, Mode=TwoWay}" Cursor=""/>
                                <CheckBox TabIndex="15" Tag="{loc:Loc Key=MainWindow.Settings.Field.EnableLoopGuard}" IsChecked="{Binding WindowSettings.EnableLoopGuard, Mode=TwoWay}" Cursor=""/>
                            </CompositeCollection>
                        </skin:PropertiesListView>
                        <Grid Background="Transparent" DataContext="{Binding GlobalSettings}">
                            <Grid.Resources>
                                <Style TargetType="{x:Type ListView}">
                                    <Setter Property="Background" Value="Transparent"/>
                                </Style>
                                <Style TargetType="{x:Type ListViewItem}">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                <Border Background="#1E2328" BorderBrush="#18488C" x:Name="Border" Margin="0,0,0,4" Padding="1" BorderThickness="1">
                                                    <ContentPresenter />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Grid.Resources>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer ScrollViewer.VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    <Grid Margin="{StaticResource TopBottomStdMargin}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition/>
                                            <RowDefinition/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Style="{StaticResource PropertyName}" Margin="{StaticResource RightStdMargin}" Text="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Title}" FontSize="14"/>
                                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                                            <Button ToolTip="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Tooltip.Add}" Margin="2,0" Style="{StaticResource TitleButton}" Command="{Binding AddItemCommand}" Content="&#x2b;"/>
                                            <Control Style="{StaticResource HelpBorder}" Margin="{StaticResource LeftRightStdMargin}" ToolTip="{loc:Loc Key=Help.StaticVariables.Default}"/>
                                        </StackPanel>
                                        <Border VerticalAlignment="Center" Grid.Column="3" BorderThickness="0,2,0,0" BorderBrush="White"/>
                                        <Grid HorizontalAlignment="Stretch" Grid.Row="1" Grid.ColumnSpan="4">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="0.1*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock HorizontalAlignment="Center" Text="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Header.Index}" />
                                            <TextBlock HorizontalAlignment="Center" Grid.Column="1" Text="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Header.Name}" />
                                            <TextBlock HorizontalAlignment="Center" Grid.Column="2" Text="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Header.Value}" />
                                        </Grid>
                                    </Grid>
                                    <ListView KeyboardNavigation.TabNavigation="Local" KeyboardNavigation.DirectionalNavigation="Contained" BorderBrush="Transparent" ItemsSource="{Binding Variables}" Background="Transparent" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" ScrollViewer.CanContentScroll="True">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <DataTemplate.Resources>
                                                    <c:IndexConverter x:Key="IndexConverter"/>
                                                </DataTemplate.Resources>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="0.1*"/>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock HorizontalAlignment="Center" Style="{StaticResource CaptionText}" FontWeight="Normal" Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListViewItem}}, Converter={StaticResource IndexConverter}}"></TextBlock>
                                                    <Border Grid.Column="1" Background="{StaticResource Main.Gradient}">
                                                        <TextBox CaretBrush="White" FontWeight="Bold" Margin="8,2,2,2" Text="{Binding A, UpdateSourceTrigger=PropertyChanged}" BorderThickness="0,0,0,1">
                                                            <i:Interaction.Triggers>
                                                                <i:EventTrigger EventName="PreviewKeyUp">
                                                                    <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                                                </i:EventTrigger>
                                                                <i:EventTrigger EventName="GotFocus">
                                                                    <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                                                </i:EventTrigger>
                                                                <i:EventTrigger EventName="LostFocus">
                                                                    <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                                                </i:EventTrigger>
                                                            </i:Interaction.Triggers>
                                                            <TextBox.Style>
                                                                <Style TargetType="TextBox">
                                                                    <Setter Property="BorderBrush" Value="Transparent"/>
                                                                    <Setter Property="Background" Value="Transparent"/>
                                                                    <Setter Property="Foreground" Value="White"/>
                                                                </Style>
                                                            </TextBox.Style>
                                                        </TextBox>
                                                    </Border>
                                                    <TextBox Background="Transparent" Foreground="White" CaretBrush="White" BorderThickness="0,0,0,1" Margin="2" Grid.Column="2" Text="{Binding B, UpdateSourceTrigger=PropertyChanged}" FontWeight="Regular">
                                                        <i:Interaction.Triggers>
                                                            <i:EventTrigger EventName="PreviewKeyUp">
                                                                <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxPreviewKeyUpCommand}"/>
                                                            </i:EventTrigger>
                                                            <i:EventTrigger EventName="GotFocus">
                                                                <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxGotFocusCommand}"/>
                                                            </i:EventTrigger>
                                                            <i:EventTrigger EventName="LostFocus">
                                                                <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding Source={x:Static Application.Current}, Path=MainWindow.DataContext.TextBoxLostFocusCommand}"/>
                                                            </i:EventTrigger>
                                                        </i:Interaction.Triggers>
                                                    </TextBox>
                                                    <Button ToolTip="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Tooltip.Remove}" CommandParameter="{Binding}" Margin="2,0" Style="{StaticResource TitleButton}" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListView}}" Content="&#xF2ED;" Grid.Column="3"/>
                                                </Grid>

                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                        <ListView.Style>
                                            <Style>
                                                <Setter Property="ListView.ItemContainerStyle">
                                                    <Setter.Value>
                                                        <Style TargetType="ListViewItem">
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="ListViewItem">
                                                                        <Border Margin="0,2">
                                                                            <ContentPresenter/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Setter Property="IsTabStop" Value="False"/>
                                                        </Style>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Variables.Count}" Value="0">
                                                        <DataTrigger.Setters>
                                                            <Setter Property="ListView.Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="ListView">
                                                                        <Border Margin="0,2">
                                                                            <TextBlock TextAlignment="Center" Style="{StaticResource CaptionText}" Text="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Empty}" />
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                        </DataTrigger.Setters>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ListView.Style>
                                    </ListView>
                                    <Button Width="Auto" Margin="2,0" Style="{StaticResource TitleButton}" FontFamily="{StaticResource SegoeUI}" Command="{Binding AddItemCommand}" Content="{loc:Loc Key=MainWindow.Settings.GlobalVariables.Button.Append}"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </StackPanel>
                </TabItem>
                <TabItem IsTabStop="False" IsSelected="True" Style="{StaticResource GrayItemStyle}" Foreground="White" >
                    <TabItem.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock VerticalAlignment="Center" Text="{loc:Loc Key=MainWindow.Tab.Layout}"/>
                            <ComboBox x:Name="Page" SelectedIndex="{Binding PageIndex, Mode=TwoWay}" IsEditable="False" MinWidth="128" Margin="8,0,0,0" ItemsSource="{StaticResource Pages}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition/>
                                            </Grid.ColumnDefinitions>
                                            <Border Width="16" Height="16" Margin="{StaticResource RightStdMargin}">
                                                <Border.Background>
                                                    <SolidColorBrush>
                                                        <SolidColorBrush.Color>
                                                            <MultiBinding  Converter="{c:ColorsListToColorConverter}">
                                                                <Binding Source="{x:Static vm:vMixWidgetSettingsViewModel.Colors}"/>
                                                                <Binding Converter="{c:ObjectToIndexConverter}" ConverterParameter="{StaticResource Pages}"/>
                                                            </MultiBinding>
                                                        </SolidColorBrush.Color>
                                                    </SolidColorBrush>
                                                </Border.Background>
                                            </Border>
                                            <TextBlock Grid.Column="12">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{c:StringListToStringConverter}">
                                                        <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.WindowSettings.Pages"/>
                                                        <Binding Converter="{c:ObjectToIndexConverter}" ConverterParameter="{StaticResource Pages}"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                        </Grid>

                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                            <Button ToolTip="{loc:Loc Key=MainWindow.Layout.Button.RenamePage.ToolTip}" Style="{StaticResource TitleButton}" Command="{Binding EditPageNameCommand}" CommandParameter="{Binding PageIndex}" IsEnabled="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}">
                                <TextBlock FontFamily="{StaticResource FontAwesome}" Text="&#xf044;"/>
                            </Button>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid x:Name="Viewfinder" ClipToBounds="False">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="2*"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>
                        <ScrollViewer Grid.RowSpan="2" Grid.ColumnSpan="2" x:Name="MainScrollViewer" Tag="{Binding IsHotkeysEnabled, Converter={wpfc:BoolToInverseBoolConverter}}"  IsManipulationEnabled="True" PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                            <ScrollViewer.Style>
                                <Style TargetType="ScrollViewer">
                                    <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                                    <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding WindowSettings.UseInfiniteCanvas}" Value="True">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                                        <Grid ClipToBounds="False" Background="{TemplateBinding Background}">
                                                            <ScrollContentPresenter ClipToBounds="False" x:Name="PART_ScrollContentPresenter" CanContentScroll="{TemplateBinding CanContentScroll}" Margin="{TemplateBinding Margin}"/>
                                                            <ScrollBar x:Name="PART_VerticalScrollBar" HorizontalAlignment="Right" VerticalAlignment="Stretch" Width="0" IsHitTestVisible="False" Visibility="Collapsed" Value="{TemplateBinding VerticalOffset}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                                                            <ScrollBar x:Name="PART_HorizontalScrollBar" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Height="0" IsHitTestVisible="False" Visibility="Collapsed" Value="{TemplateBinding HorizontalOffset}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                                                        </Grid>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="HorizontalScrollBarVisibility" Value="Hidden"/>
                                            <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
                                            <Setter Property="ClipToBounds" Value="False"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ScrollViewer.Style>
                            <Grid Name="LayoutGrid" ClipToBounds="True" PreviewMouseDown="LayoutGrid_PreviewMouseDown" PreviewMouseMove="LayoutGrid_PreviewMouseMove" PreviewMouseUp="LayoutGrid_PreviewMouseUp" PreviewMouseWheel="LayoutGrid_PreviewMouseWheel">
                                <Grid.Style>
                                    <Style TargetType="{x:Type Grid}">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <ImageBrush x:Name="CanvasGridBrush" ImageSource="Grid.png" TileMode="Tile" ViewportUnits="Absolute" Viewport="0 0 8 8"/>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding WindowSettings.Locked}" Value="True">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <SolidColorBrush Color="#FF1E2328"/>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ContextMenu>
                                    <ContextMenu x:Name="LayoutGridContextMenu" DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}" VerticalContentAlignment="Center" HorizontalContentAlignment="Left" ItemsSource="{DynamicResource CreateItems}">
                                        <ContextMenu.Resources>
                                            <cls:BindingProxy x:Key="proxy" Data="{Binding}" />
                                        </ContextMenu.Resources>
                                        <ContextMenu.ItemContainerStyleSelector>
                                            <cls:MenuStyleSelector>
                                                <cls:MenuStyleSelector.MenuItem>
                                                    <Style TargetType="MenuItem">
                                                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                        <Setter Property="IsEnabled" Value="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}"/>
                                                    </Style>
                                                </cls:MenuStyleSelector.MenuItem>
                                            </cls:MenuStyleSelector>
                                        </ContextMenu.ItemContainerStyleSelector>
                                    </ContextMenu>
                                </Grid.ContextMenu>
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="MouseLeftButtonDown">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding MouseButtonDownCommand}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="MouseMove">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding MouseMoveCommand}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="MouseLeftButtonUp">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding MouseButtonUpCommand}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="ContextMenuOpening">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding ContextMenuOpeningCommand}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="ContextMenuClosing">
                            <i:InvokeCommandAction PassEventArgsToCommand="True" Command="{Binding ContextMenuClosingCommand}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>

                                <Canvas IsHitTestVisible="False">
                                    <Grid Width="{Binding ElementName=MainScrollViewer, Path=ViewportWidth}" Height="{Binding ElementName=MainScrollViewer, Path=ViewportHeight}"
							  Canvas.Left="{Binding ElementName=MainScrollViewer, Path=HorizontalOffset}" Canvas.Top="{Binding ElementName=MainScrollViewer, Path=VerticalOffset}">

                                        <TextBlock Margin="0,0,16,2" FontFamily="Segoe UI" FontWeight="Bold" Opacity="0.25" FontSize="120" VerticalAlignment="Bottom" HorizontalAlignment="Right" IsHitTestVisible="False">
                                            <TextBlock.Text>
                                                <MultiBinding Converter="{c:StringListToStringConverter}">
                                                    <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.WindowSettings.Pages"/>
                                                    <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.PageIndex"/>
                                                </MultiBinding>
                                            </TextBlock.Text>
                                            <TextBlock.Foreground>
                                                <SolidColorBrush>
                                                    <SolidColorBrush.Color>
                                                        <MultiBinding Converter="{c:ColorsListToColorConverter}">
                                                            <Binding Source="{x:Static vm:vMixWidgetSettingsViewModel.Colors}"/>
                                                            <Binding RelativeSource="{RelativeSource AncestorType=Window}" Path="DataContext.PageIndex"/>
                                                        </MultiBinding>
                                                    </SolidColorBrush.Color>
                                                </SolidColorBrush>
                                            </TextBlock.Foreground>
                                        </TextBlock>
                                    </Grid>
                                </Canvas>

                                <Grid x:Name="CanvasRoot" Background="Transparent">
                                    <Grid x:Name="CanvasContent">
                                        <Grid.Style>
                                            <Style TargetType="Grid">
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <MatrixTransform/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding WindowSettings.UseInfiniteCanvas}" Value="False">
                                                        <Setter Property="RenderTransform">
                                                            <Setter.Value>
                                                                <MatrixTransform Matrix="Identity"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Grid.Style>
                                        <Path Visibility="{Binding WindowSettings.UseInfiniteCanvas, Converter={wpfc:BoolToVisibilityConverter}}" Stroke="White" 
                                          StrokeThickness="2" 
                                          StrokeStartLineCap="Round" 
                                          StrokeEndLineCap="Round"
                                          Data="M 0,0 L 4,0 M 0,0 L 0,4" 
                                          Stretch="Uniform" 
                                          Width="8" Height="8" 
                                          HorizontalAlignment="Left" 
                                          VerticalAlignment="Top" Opacity="0.5"/>
                                        <Path Visibility="{Binding WindowSettings.UseInfiniteCanvas, Converter={wpfc:BoolToVisibilityConverter}}" Stroke="White" 
                                          StrokeThickness="2" 
                                          StrokeStartLineCap="Round" 
                                          StrokeEndLineCap="Round"
                                          Data="M 4,4 L 4,0 M 4,4 L 0,4" 
                                          Stretch="Uniform" 
                                          Width="8" Height="8" 
                                          HorizontalAlignment="Right" 
                                          VerticalAlignment="Bottom" Opacity="0.5"/>
                                        <Path Visibility="{Binding WindowSettings.UseInfiniteCanvas, Converter={wpfc:BoolToVisibilityConverter}}" Stroke="White" 
                                          StrokeThickness="2" 
                                          StrokeStartLineCap="Round" 
                                          StrokeEndLineCap="Round"
                                          Data="M 4,0 L 0,0 M 4,0 L 4,4" 
                                          Stretch="Uniform" 
                                          Width="8" Height="8" 
                                          HorizontalAlignment="Right" 
                                          VerticalAlignment="Top" Opacity="0.5"/>
                                        <Path Visibility="{Binding WindowSettings.UseInfiniteCanvas, Converter={wpfc:BoolToVisibilityConverter}}" Stroke="White" 
                                          StrokeThickness="2" 
                                          StrokeStartLineCap="Round" 
                                          StrokeEndLineCap="Round"
                                          Data="M 0,4 L 0,0 M 0,4 L 4,4" 
                                          Stretch="Uniform" 
                                          Width="8" Height="8" 
                                          HorizontalAlignment="Left" 
                                          VerticalAlignment="Bottom" Opacity="0.5"/>
                                        <ListView e:ListViewSortBehavior.IsSortable="True" e:ListViewSortBehavior.SortableItemsSource="{Binding Widgets}" e:ListViewSortBehavior.DefaultSortDirection="Ascending" e:ListViewSortBehavior.DefaultSortProperty="ZIndex" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Standard" VirtualizingPanel.IsContainerVirtualizable="True" x:Name="Layout" BorderThickness="0" Background="Transparent" ContextMenuOpening="Layout_ContextMenuOpening" ClipToBounds="False">
                                            <ListView.Template>
                                                <ControlTemplate TargetType="ListView">
                                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                        <ItemsPresenter/>
                                                    </Border>
                                                </ControlTemplate>
                                            </ListView.Template>
                                            <ListView.ContextMenu>
                                                <ContextMenu x:Name="CreateContextMenu" VerticalContentAlignment="Center" HorizontalContentAlignment="Left" ItemsSource="{DynamicResource CreateItems}">
                                                    <ContextMenu.Resources>
                                                        <cls:BindingProxy x:Key="proxy" Data="{Binding}" />
                                                    </ContextMenu.Resources>
                                                    <ContextMenu.ItemContainerStyleSelector>
                                                        <cls:MenuStyleSelector>
                                                            <cls:MenuStyleSelector.MenuItem>
                                                                <Style TargetType="MenuItem">
                                                                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                                                                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                                                                    <Setter Property="IsEnabled" Value="{Binding WindowSettings.Locked, Converter={wpfc:BoolToInverseBoolConverter}}"/>
                                                                </Style>
                                                            </cls:MenuStyleSelector.MenuItem>
                                                        </cls:MenuStyleSelector>
                                                    </ContextMenu.ItemContainerStyleSelector>
                                                </ContextMenu>
                                            </ListView.ContextMenu>
                                            <ListView.Resources>
                                                <ResourceDictionary>
                                                    <ResourceDictionary.MergedDictionaries>
                                                        <ResourceDictionary Source="Skins/ControlTemplates.xaml"/>
                                                    </ResourceDictionary.MergedDictionaries>
                                                </ResourceDictionary>
                                            </ListView.Resources>
                                            <ListView.Style>
                                                <Style TargetType="ListView">
                                                    <Setter Property="ItemTemplateSelector">
                                                        <Setter.Value>
                                                            <ctrls:TypeTemplateSelector Page="0"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListView.Style>

                                            <ListView.Cursor>
                                                <Binding Converter="{c:StringToCursorConverter}" Mode="OneWay" Path="EditorCursor"/>
                                            </ListView.Cursor>

                                            <i:Interaction.Behaviors>
                                                <e:IgnoreMouseWheelBehavior IgnoreBehavior="{Binding IsHotkeysEnabled, Converter={wpfc:BoolToInverseBoolConverter}}"/>
                                            </i:Interaction.Behaviors>
                                            <ListView.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <Grid VirtualizingPanel.IsVirtualizing="True" ScrollViewer.CanContentScroll="True" IsItemsHost="True" HorizontalAlignment="Left" VerticalAlignment="Top">
                                                        <Grid.LayoutTransform>
                                                            <ScaleTransform ScaleX="{Binding DataContext.WindowSettings.UIScale, ElementName=Window}" ScaleY="{Binding DataContext.WindowSettings.UIScale, ElementName=Window}"/>
                                                        </Grid.LayoutTransform>
                                                    </Grid>
                                                </ItemsPanelTemplate>
                                            </ListView.ItemsPanel>
                                            <ListView.ItemContainerStyle>
                                                <Style TargetType="{x:Type ListViewItem}">
                                                    <Setter Property="Margin">
                                                        <Setter.Value>
                                                            <MultiBinding Converter="{c:DoubleToThicknessConverter}">
                                                                <Binding Path="Left"/>
                                                                <Binding Path="Top"/>
                                                            </MultiBinding>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Setter Property="HorizontalAlignment" Value="Left"/>
                                                    <Setter Property="VerticalAlignment" Value="Top"/>
                                                    <!--</Setter> Value="{Binding Left}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Top}"/>-->
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ListViewItem}">
                                                                <ContentPresenter />
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListView.ItemContainerStyle>
                                        </ListView>
                                        <!--Visibility="{Binding WindowSettings.ShowLinks, Converter={wpfc:BoolToVisibilityConverter}}"-->
                                        <ctrls:WidgetLinksOverlay x:Name="ConnectionsOverlay" Items="{Binding Widgets}" CurrentPage="{Binding PageIndex}" PopupHost="{Binding ElementName=Viewfinder}" ScrollHost="{Binding ElementName=MainScrollViewer}" Mode="{Binding WindowSettings.LinksStyle}"/>
                                    </Grid>
                                </Grid>

                                <Grid Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="{Binding SelectorPosition}" Width="{Binding SelectorWidth}" Height="{Binding SelectorHeight}" Visibility="{Binding SelectorEnabled, Converter={StaticResource BooleanToVisibilityConverter}}" IsHitTestVisible="False" Background="#7F00FFFF"/>

                                <Border BorderBrush="White" BorderThickness="2" Opacity="0" Background="DarkSlateGray" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="2" Margin="2" IsHitTestVisible="False">
                                    <TextBlock FontSize="18" TextAlignment="Center" Text="{loc:Loc Key=MainWindow.Layout.Hint}" />
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding EditorCursor}" Value="Arrow">
                                                    <DataTrigger.EnterActions>
                                                        <BeginStoryboard>
                                                            <Storyboard Storyboard.TargetProperty="Opacity">
                                                                <DoubleAnimation To="0" Duration="0:0:0.5"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.EnterActions>
                                                    <DataTrigger.ExitActions>
                                                        <BeginStoryboard>
                                                            <Storyboard Storyboard.TargetProperty="Opacity">
                                                                <DoubleAnimation To="1" Duration="0:0:0.5"/>
                                                            </Storyboard>
                                                        </BeginStoryboard>
                                                    </DataTrigger.ExitActions>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                </Border>
                            </Grid>
                        </ScrollViewer>
                        <Border Visibility="Hidden" Opacity="0" ClipToBounds="True" x:Name="MiniMapBorder" Grid.Row="2" Grid.Column="2" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="20" BorderBrush="White" BorderThickness="1" Background="#80000000" CornerRadius="3" IsHitTestVisible="False">
                            <Grid Margin="2">
                                <Grid>
                                    <Grid.Background>
                                        <VisualBrush Visual="{Binding ElementName=CanvasContent}" Stretch="Uniform"/>
                                    </Grid.Background>
                                </Grid>
                                <Viewbox Stretch="Uniform">
                                    <Canvas Width="{Binding ElementName=CanvasRoot, Path=ActualWidth}" Height="{Binding ElementName=CanvasRoot, Path=ActualHeight}">
                                        <Border BorderBrush="Red" BorderThickness="{Binding ElementName=CanvasContent, Path=RenderTransform.Matrix, Converter={c:MatrixToThicknessConverter}}" Width="{Binding ElementName=Viewfinder, Path=ActualWidth}" Height="{Binding ElementName=Viewfinder, Path=ActualHeight}">
                                            <Border.RenderTransform>
                                                <MatrixTransform>
                                                    <MatrixTransform.Matrix>
                                                        <MultiBinding Converter="{c:MatrixInverterWidgetsConverter}">
                                                            <Binding ElementName="CanvasContent" Path="RenderTransform.Matrix"/>
                                                            <Binding Path="Widgets"/>
                                                            <Binding ElementName="CanvasRoot" Path="ActualWidth"/>
                                                            <Binding ElementName="CanvasRoot" Path="ActualHeight"/>
                                                        </MultiBinding>
                                                    </MatrixTransform.Matrix>
                                                </MatrixTransform>
                                            </Border.RenderTransform>
                                        </Border>
                                    </Canvas>
                                </Viewbox>
                                <TextBlock Text="{Binding ElementName=CanvasContent, Path=RenderTransform.Matrix.M11, StringFormat={}{0:P0}}" 
                   Foreground="White" 
                   FontSize="10" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Bottom" 
                   Margin="5"
                   FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <DropShadowEffect BlurRadius="2" ShadowDepth="1"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <Rectangle Width="4" Height="16" Margin="{StaticResource RightStdMargin}" Stroke="Transparent">
                        <Rectangle.Fill>
                            <SolidColorBrush Color="{Binding Pollingstatus, Converter={c:StatusToColorConverter}}"/>
                        </Rectangle.Fill>
                    </Rectangle>
                    <TextBlock Margin="0,0,8,0">
                        HTTP://<Run FontWeight="Bold" Text="{Binding WindowSettings.IP}"/>:<Run FontWeight="Bold" Text="{Binding WindowSettings.Port}"/>
                    </TextBlock>
                    <Ellipse Width="16" Height="16" Margin="{StaticResource RightStdMargin}" Stroke="Black">
                        <Ellipse.Fill>
                            <SolidColorBrush Color="{Binding Status, Converter={c:StatusToColorConverter}}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <TextBlock>
                        <TextBlock.Text>
                            <MultiBinding ConverterParameter="Enum.Connection">
                                <Binding Path="Status"/>
                                <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Culture"/>
                                <MultiBinding.Converter>
                                    <c:EnumLocalizationMultiConverter/>
                                </MultiBinding.Converter>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem  HorizontalAlignment="Right" HorizontalContentAlignment="Right">
                <CheckBox IsChecked="{Binding LIVE, Mode=TwoWay}" FontWeight="Bold" Width="64" HorizontalContentAlignment="Center" Content="{loc:Loc Key=MainWindow.StatusBar.Live}">
                    <CheckBox.Style>
                        <Style TargetType="CheckBox">
                            <Style.Setters>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="CheckBox">
                                            <Grid x:Name="back" Background="Red">
                                                <ContentPresenter HorizontalAlignment="Center" Margin="2"/>
                                            </Grid>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter Property="Background" Value="Yellow" TargetName="back"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style.Setters>
                        </Style>
                    </CheckBox.Style>
                </CheckBox>
            </StatusBarItem>

            <!--<StatusBarItem  HorizontalAlignment="Right" HorizontalContentAlignment="Right">
                <CheckBox IsChecked="{Binding WindowSettings.IsTopmost, Mode=TwoWay}">Is Topmost</CheckBox>
            </StatusBarItem>
            <StatusBarItem  HorizontalAlignment="Right" HorizontalContentAlignment="Right">
                <CheckBox IsChecked="{Binding WindowSettings.EnableLog, Mode=TwoWay}">Enable Log</CheckBox>
            </StatusBarItem>-->
            <StatusBarItem HorizontalAlignment="Right" HorizontalContentAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Button Margin="0,0,8,0" Command="{Binding StopAllScriptsCommand}" Content="{loc:Loc Key=MainWindow.StatusBar.StopScripts}"/>
                    <!--<CheckBox IsChecked="{Binding WindowSettings.ShowLinks}" Content="{loc:Loc Key=MainWindow.StatusBar.ShowLinks}" Margin="0,0,8,0"/>-->
                    <TextBlock VerticalAlignment="Center" Margin="0,0,8,0" Text="{loc:Loc Key=MainWindow.StatusBar.LinksStyle}"/>
                    <ComboBox MinWidth="128" Margin="0,0,8,0" SelectedValue="{Binding WindowSettings.LinksStyle, UpdateSourceTrigger=Default, Mode=TwoWay}" ItemsSource="{Binding Source={x:Static cls:Constants.Links}, Converter={c:StringArrayToLocalizedArrayConverter}, ConverterParameter=MainWindow.StatusBar.LinksType}" DisplayMemberPath="Localized" SelectedValuePath="Original">
                        
                    </ComboBox>
                    <CheckBox Margin="0,0,8,0" IsChecked="{Binding IsGhosted, Mode=TwoWay}" ToolTip="{loc:Loc Key=MainWindow.StatusBar.Ghosted.ToolTip}" IsEnabled="{Binding WindowSettings.Locked, Mode=OneWay, Converter={wpfc:BoolToInverseBoolConverter}}">
                        <CheckBox.Style>
                            <Style TargetType="CheckBox">
                                <Style.Setters>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="CheckBox">
                                                <Grid Background="Aqua">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock FontFamily="{StaticResource FontAwesome}" VerticalAlignment="Center" x:Name="Icon"/>
                                                        <ContentPresenter VerticalAlignment="Center" Margin="2"/>
                                                    </StackPanel>
                                                </Grid>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="Foreground" Value="Gray"/>
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter Property="Text" Value="&#xf06e;" TargetName="Icon"/>
                                                        <Setter Property="Content">
                                                            <Setter.Value>
                                                                <MultiBinding>
                                                                    <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Culture"/>
                                                                    <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Item[MainWindow.StatusBar.Ghosted]"/>
                                                                    <MultiBinding.Converter>
                                                                        <c:LastOfMultiBindingConverter/>
                                                                    </MultiBinding.Converter>
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter Property="Text" Value="&#xf070;" TargetName="Icon"/>
                                                        <Setter Property="Content">
                                                            <Setter.Value>
                                                                <MultiBinding>
                                                                    <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Culture"/>
                                                                    <Binding Source="{x:Static loc:LocalizationManager.Instance}" Path="Item[MainWindow.StatusBar.Visible]"/>
                                                                    <MultiBinding.Converter>
                                                                        <c:LastOfMultiBindingConverter/>
                                                                    </MultiBinding.Converter>
                                                                </MultiBinding>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style.Setters>
                            </Style>
                        </CheckBox.Style>
                    </CheckBox>
                    <!--<Button Margin="0,0,8,0" Command="{Binding RegisterNDIFilters}">Register Filters</Button>-->
                    <TextBlock>
                    <Hyperlink ToolTip="{loc:Loc Key=MainWindow.StatusBar.Donate.ToolTip}" NavigateUri="https://coindrop.to/elgarf" RequestNavigate="Hyperlink_RequestNavigate">
                        <Image Height="18px" Source="/vMixControllerSkin;component\btn_donate_SM.png" Stretch="Uniform" Cursor="Hand"/>
                    </Hyperlink>
                    <Hyperlink>
                         
                    </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
        <Border x:Name="Loading" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.RowSpan="3" Cursor="Wait" Visibility="Collapsed">
            <Border.Background>
                <DrawingBrush Viewport="0,0,4,4" ViewportUnits="Absolute" TileMode="Tile">
                    <DrawingBrush.Drawing>
                        <DrawingGroup>
                            <GeometryDrawing Brush="{StaticResource Main.Background}">
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <RectangleGeometry Rect="0,0,8,8" />
                                        <RectangleGeometry Rect="8,8,8,8" />
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                            <GeometryDrawing Brush="#AA000000">
                                <GeometryDrawing.Geometry>
                                    <GeometryGroup>
                                        <RectangleGeometry Rect="8,0,8,8" />
                                        <RectangleGeometry Rect="0,8,8,8" />
                                    </GeometryGroup>
                                </GeometryDrawing.Geometry>
                            </GeometryDrawing>
                        </DrawingGroup>
                    </DrawingBrush.Drawing>
                </DrawingBrush>
            </Border.Background>
            <Border Width="256" VerticalAlignment="Center" HorizontalAlignment="Center" Background="{StaticResource Main.Background}" BorderBrush="{StaticResource Main.Foreground}" BorderThickness="4" Padding="{StaticResource StdMargin}">
                <StackPanel>
                    <TextBlock HorizontalAlignment="Center" FontFamily="/vMixControllerSkin;Component/#Segoe UI Mono" Foreground="White" FontSize="48" Text="{loc:Loc Key=MainWindow.Loading}"/>
                    <ProgressBar BorderThickness="0" Background="Transparent" Foreground="White" IsIndeterminate="True" Height="16"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>






