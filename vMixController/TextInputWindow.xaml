<Window x:Class="vMixController.TextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:vMixController"
        mc:Ignorable="d"
        x:Name="Me"
        Title="Input Page Name" Height="450" Width="600" ResizeMode="NoResize" WindowStyle="ToolWindow" SizeToContent="Height" Background="#1e2328" WindowStartupLocation="CenterScreen" Activated="Me_Activated">
    <Window.Resources>
        <ResourceDictionary Source="/vMixControllerSkin;component/MainSkin.xaml"/>
    </Window.Resources>
    <StackPanel Margin="4">
        <TextBox x:Name="tb" Style="{StaticResource WidgetTextBox}" Text="{Binding Text, ElementName=Me}"/>
        <Grid Margin="{StaticResource StdMargin}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button IsCancel="True" Grid.Column="1" Margin="{StaticResource LeftRightStdMargin}">_Cancel</Button>
            <Button IsDefault="True" Grid.Column="2" Click="Button_Click">_OK</Button>
        </Grid>
    </StackPanel>
</Window>
